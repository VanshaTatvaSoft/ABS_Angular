import { TimeFormatPipePipe } from './time-format-pipe-pipe';

describe('TimeFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
